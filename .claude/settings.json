{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; echo \"$f\" | grep -q '\\.php$' && php vendor/bin/phpcs \"$f\" --colors 2>/dev/null || true; }",
            "statusMessage": "Checking code style (phpcs)..."
          }
        ]
      }
    ]
  }
}
